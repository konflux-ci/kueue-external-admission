---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: externaladmissionconfigs.konflux-ci.dev
spec:
  group: konflux-ci.dev
  names:
    kind: ExternalAdmissionConfig
    listKind: ExternalAdmissionConfigList
    plural: externaladmissionconfigs
    singular: externaladmissionconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalAdmissionConfig is the Schema for the externaladmissionconfigs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExternalAdmissionConfigSpec defines the desired state of
              ExternalAdmissionConfig.
            properties:
              provider:
                description: |-
                  Provider contains the configuration for the admission check provider
                  Exactly one provider must be configured
                maxProperties: 1
                minProperties: 1
                properties:
                  alertManager:
                    description: AlertManager contains AlertManager-specific configuration
                    properties:
                      alertFilters:
                        description: |-
                          AlertFilters contains the list of alert filtering configurations
                          Each filter can be applied to different subsets of workloads
                        items:
                          description: AlertFiltersConfig contains alert filtering
                            configuration
                          properties:
                            alertNames:
                              description: AlertNames is a list of alert names to
                                watch for
                              items:
                                type: string
                              type: array
                            labelSelectors:
                              description: LabelSelectors for more complex alert filtering
                              items:
                                description: LabelSelector contains label-based selection
                                  criteria
                                properties:
                                  name:
                                    description: Name of the label
                                    type: string
                                  operator:
                                    default: equals
                                    description: Operator for comparison (equals,
                                      notEquals, regex)
                                    enum:
                                    - equals
                                    - notEquals
                                    - regex
                                    type: string
                                  value:
                                    description: Value of the label (supports regex)
                                    type: string
                                required:
                                - name
                                - value
                                type: object
                              type: array
                          type: object
                        type: array
                      connection:
                        description: Connection contains AlertManager connection details
                        properties:
                          bearerToken:
                            description: BearerToken configuration for AlertManager
                              connection
                            properties:
                              tokenFile:
                                description: TokenFile is the path to a file containing
                                  the bearer token
                                type: string
                            required:
                            - tokenFile
                            type: object
                          timeout:
                            default: 10s
                            description: Timeout for AlertManager API calls
                            type: string
                          tls:
                            description: TLS configuration for AlertManager connection
                            properties:
                              caFile:
                                description: CAFile is the path to the CA certificate
                                  file
                                type: string
                              certFile:
                                description: CertFile is the path to the client certificate
                                  file
                                type: string
                              insecureSkipVerify:
                                description: InsecureSkipVerify controls whether to
                                  skip certificate verification
                                type: boolean
                              keyFile:
                                description: KeyFile is the path to the client private
                                  key file
                                type: string
                            type: object
                          url:
                            description: URL is the AlertManager API endpoint
                            type: string
                        required:
                        - url
                        type: object
                      syncConfig:
                        description: SyncConfig contains the configuration for syncing
                          with AlertManager
                        properties:
                          interval:
                            default: 30s
                            description: Interval is the duration between sync operations
                            type: string
                        type: object
                    required:
                    - alertFilters
                    - connection
                    type: object
                type: object
            required:
            - provider
            type: object
          status:
            description: ExternalAdmissionConfigStatus defines the observed state
              of ExternalAdmissionConfig.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the config's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectedAdmissionChecks:
                description: ConnectedAdmissionChecks lists the AdmissionChecks using
                  this config
                items:
                  type: string
                type: array
              lastSyncTime:
                description: LastSyncTime is when the configuration was last successfully
                  applied
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
